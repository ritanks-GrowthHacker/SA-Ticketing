### Update Organization Table with New Columns
POST {{baseUrl}}/api/update-organization-table
Content-Type: application/json

{}

### Expected Response:
# This will add all the missing columns to the organizations table:
# - username (VARCHAR, UNIQUE)
# - password_hash (VARCHAR) 
# - org_email (VARCHAR, UNIQUE)
# - mobile_number (VARCHAR)
# - otp (VARCHAR)
# - otp_expiry (TIMESTAMP)
# - otp_verified (BOOLEAN)
# - associated_departments (UUID[])
#
# Status: 200 OK
# {
#   "success": true,
#   "message": "Organization table update completed",
#   "results": [...],
#   "tableStructure": ["id", "name", "domain", "username", "password_hash", "org_email", ...]
# }